import { CountMarketDaysPipe } from './count-market-days-pipe';

describe('CountMarketDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new CountMarketDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
